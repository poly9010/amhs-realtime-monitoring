# 02. 데이터 명세

> **현재 대부분 [문서 기준 가정]입니다.** 1주차에 직접 확인하고 다른 부분은 고치세요.
> 확인한 항목은 앞에 ✅를 붙입니다.

## 1. 출처
- NASA C-MAPSS Turbofan Engine Degradation Simulation Data Set
- NASA PCoE 데이터 저장소 또는 Kaggle에서 다운로드 가능
- 다운로드 날짜: (기입) / 용량: (기입)

## 2. 파일 구성 [가정]

하위셋마다 파일 3개, 총 12개 + 설명 문서(readme.txt)

| 파일 | 내용 |
|---|---|
| `train_FD00X.txt` | 엔진별 **고장까지의 전체 운전 이력** |
| `test_FD00X.txt` | 엔진별 이력이 **고장 전 임의 시점에서 잘림** |
| `RUL_FD00X.txt` | test 각 엔진의 **마지막 시점 잔여수명 정답** (한 줄에 하나) |

- 형식: 공백 구분 텍스트, **헤더 없음**
- 컬럼 순서: `engine_id, cycle, op_setting_1, op_setting_2, op_setting_3, sensor_1 ... sensor_21` (총 26열)

```python
cols = ["engine_id","cycle","op1","op2","op3"] + [f"s{i}" for i in range(1,22)]
df = pd.read_csv(path, sep=r"\s+", header=None, names=cols)
```

## 3. 하위셋 특성 [가정 — 직접 확인]

| 셋 | 운전조건 | 고장모드 | train 엔진 | test 엔진 | 난이도 |
|---|---|---|---|---|---|
| FD001 | 1 | 1 (HPC 열화) | 100 | 100 | 쉬움 |
| FD002 | 6 | 1 | 260 | 259 | 중간 |
| FD003 | 1 | 2 (HPC, Fan) | 100 | 100 | 중간 |
| FD004 | 6 | 2 | 249 | 248 | 어려움 |

⚠️ 공식 readme와 실제 파일의 엔진 수가 다른 사례가 보고됨. **반드시 `df.engine_id.nunique()`로 직접 확인**하고 이 표를 고칠 것.

## 4. 1주차 확인 체크리스트
- [ ] 각 파일의 행 수, 열 수, 엔진 수
- [ ] 엔진별 사이클 길이 분포 (최소/중앙값/최대)
- [ ] RUL 파일 줄 수 = test 엔진 수 인지
- [ ] op_setting 3개의 실제 값 분포 → 운전조건이 몇 종류인지
- [ ] 센서 21개 중 값이 거의 변하지 않는 센서 목록
- [ ] 결측·중복 여부
- [ ] readme.txt의 설명과 실제 데이터의 차이

## 5. 확인 결과 (1주차에 채우기)

| 셋 | 행 수 | train 엔진 | test 엔진 | 사이클 중앙값 | 비고 |
|---|---|---|---|---|---|
| FD001 | | | | | |
| FD002 | | | | | |
| FD003 | | | | | |
| FD004 | | | | | |

- 변화 없는 센서:
- 운전조건 군집 수:
- 기타 특이사항:

## 6. 센서 의미 [참고]
센서 21개는 온도·압력·회전속도·연료비 등 엔진 내부 측정값이며, 원 논문 기준 이름은 공개되어 있으나 **이름을 몰라도 분석에 지장은 없다.** 열화 경향이 뚜렷한 센서를 EDA로 직접 찾는 편이 실전에 가깝다.
- 확인한 센서별 경향은 아래에 기록:

## 7. 파생 데이터
- `data/processed/cmapss.parquet`: 4개 셋을 합치고 `subset` 컬럼 추가
- `RUL` 라벨: train은 엔진별 최대 사이클 − 현재 사이클, 상한값은 3주차에 결정 (D-라벨)

## 8. 가공·분할 규칙
- 실시간 재생 시연은 **test 데이터**로 한다 (모델이 보지 못한 데이터)
- train 내부에서 검증셋을 나눌 때는 **엔진 단위**로 나눈다 (같은 엔진의 사이클이 학습·검증에 섞이면 누수)
