# 04. 코드 구조

> 코드 구조나 주요 함수가 바뀌면 갱신합니다. Claude가 새 코드를 짤 때 기존 구조와 이름을 맞추는 기준이 됩니다.

## 1. 환경
- Python: (버전 기입, 예: 3.11)
- 주요 패키지: pandas, numpy, polars, duckdb, pyarrow, matplotlib, plotly, scikit-learn, lightgbm, shap, streamlit, (선택) torch
- 가상환경: (venv / conda 기입)
- 저장소: https://github.com/poly9010/phm-ion-mill-predictive-maintenance
- 로컬 사양: (RAM __GB, GPU 유무)

## 2. 폴더 구조 (계획)
```
phm2018-predictive-maintenance/
├── data/
│   ├── raw/            # 원본 CSV (git 제외)
│   ├── processed/      # Parquet, 요약 특징 (git 제외)
│   └── README.md       # 데이터 다운로드 방법
├── notebooks/
│   ├── 01_data_overview.ipynb
│   ├── 02_eda_normal_pattern.ipynb
│   ├── 03_eda_fault_precursor.ipynb
│   ├── 04_baseline.ipynb
│   ├── 05_anomaly_detection.ipynb
│   ├── 06_classification.ipynb
│   ├── 07_ttf_regression.ipynb
│   └── 08_evaluation_shap.ipynb
├── src/
│   ├── config.py       # 경로, 상수
│   ├── io.py           # CSV→Parquet 변환, 데이터 로드
│   ├── labeling.py     # 고장 라벨 생성
│   ├── features.py     # 특징 추출
│   ├── split.py        # 시간순/장비별 분할
│   ├── models/         # 모델 학습·추론
│   └── evaluation.py   # 오경보율, 리드타임 등 평가 지표
├── app/
│   └── dashboard.py    # Streamlit 대시보드
├── reports/figures/    # README용 그래프
├── requirements.txt
└── README.md
```

## 3. 주요 모듈·함수 (구현하면서 채우기)

| 파일 | 함수 | 역할 | 입력 → 출력 |
|---|---|---|---|
| src/io.py | `csv_to_parquet()` | 장비별 CSV를 Parquet으로 변환 | CSV 경로 → Parquet 파일 |
| | | | |

## 4. 코딩 규칙
- 노트북은 탐색용, 재사용 코드는 `src/`로 옮김
- 경로와 상수는 `config.py`에서만 관리
- 원본 데이터(`data/raw`, `data/processed`)는 `.gitignore`에 추가
- 난수 시드 고정 (예: 42)
- 그래프는 `reports/figures/`에 저장해서 README에 사용

## 5. 알려진 이슈·메모
- 
