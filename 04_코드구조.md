# 04. 코드 구조

> 구조가 바뀌면 갱신합니다. Claude가 새 코드를 짤 때 이름과 구조를 맞추는 기준입니다.
> 아래는 **계획안**이며, 진행하면서 실제 구조에 맞게 고칩니다.

## 1. 환경
- Python: (기입)
- 분석·모델: pandas, numpy, pyarrow, matplotlib, plotly, scikit-learn, lightgbm, shap
- 시스템: streamlit, paho-mqtt, influxdb-client, Docker Compose
- 선택: torch (시퀀스 모델), onnxruntime (경량화)
- 저장소: (GitHub URL 기입) / 이름 후보: `fleet-rul-monitoring`
- 로컬 사양: (RAM, GPU 유무)

## 2. 폴더 구조 (계획)
```
fleet-rul-monitoring/
├── data/
│   ├── raw/                  # C-MAPSS 원본 txt (git 제외)
│   ├── processed/            # cmapss.parquet 등 (git 제외)
│   └── README.md             # 다운로드·준비 방법
├── notebooks/
│   ├── 01_load_check.ipynb   # 구조 확인
│   ├── 02_eda.ipynb
│   ├── 03_labeling.ipynb
│   ├── 04_baseline.ipynb
│   └── 05_models.ipynb
├── src/
│   ├── config.py             # 경로, 컬럼명, 상수
│   ├── load.py               # txt 로드 + 컬럼 부여 + Parquet 저장
│   ├── labeling.py           # RUL 라벨 생성 (상한 처리 포함)
│   ├── features.py           # 롤링 특징 (학습·실시간 공용)
│   ├── train.py
│   ├── evaluate.py           # RMSE, PHM 점수, 조기 감지 등
│   └── policy.py             # 알람 기준, 정비 우선순위
├── app_mvp/
│   └── dashboard.py          # 8주차 Streamlit MVP
├── services/                 # 9주차 이후
│   ├── replayer/             # test 엔진 사이클 단위 재생 → MQTT
│   ├── inference/            # 구독 → 특징 → 예측 → InfluxDB
│   ├── grafana/
│   └── mosquitto/
├── models/
├── reports/figures/
├── docker-compose.yml
└── README.md
```

## 3. 데이터 흐름 (계획)
```
[학습]   raw txt → load.py → cmapss.parquet → labeling.py → features.py → train.py → models/
[실시간] test 엔진 → replayer (1 사이클 = 1 틱, 배속 조절)
         → MQTT(fleet/{subset}/{engine_id}) → inference (features.py + 모델 + policy.py)
         → InfluxDB → Grafana / Streamlit
```
- 메시지 예: `{"subset":"FD001","engine":42,"cycle":137,"op1":..., "s1":..., ...}`

## 4. 주요 모듈 (구현하면서 채우기)

| 파일 | 함수 | 역할 | 상태 |
|---|---|---|---|
| src/load.py | `load_subset()` | txt → DataFrame (컬럼 부여) | 미구현 |
| src/labeling.py | `add_rul()` | 엔진별 RUL 계산 + 상한 적용 | 미구현 |
| src/features.py | `make_features()` | 최근 N사이클 롤링 특징 | 미구현 |
| src/evaluate.py | `phm_score()` | 비대칭 점수 (늦은 예측에 큰 벌점) | 미구현 |
| src/policy.py | `maintenance_queue()` | RUL 기준 정비 우선순위 | 미구현 |

## 5. 코딩 규칙
- 노트북은 탐색용, 재사용 코드는 src/로 이동
- **특징 계산과 정책은 학습·실시간이 같은 함수를 사용** (불일치 방지)
- 경로·상수는 config.py에서만 관리
- data/, models/ 대용량 파일은 .gitignore
- 난수 시드 42 고정
- 그래프는 reports/figures/에 저장

## 6. 알려진 이슈·메모
-
