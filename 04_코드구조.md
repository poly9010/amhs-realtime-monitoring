# 04. 코드 구조

> 코드 구조나 주요 함수가 바뀌면 갱신합니다. Claude가 새 코드를 짤 때 기존 구조와 이름을 맞추는 기준이 됩니다.

## 1. 환경
- Python: (버전 기입, 예: 3.11)
- 분석·모델: pandas, pyarrow, numpy, matplotlib, plotly, scikit-learn, lightgbm, shap, onnx, onnxruntime
- 시스템: streamlit, paho-mqtt, influxdb-client, Docker, Docker Compose
- 인프라(컨테이너): Mosquitto(MQTT), InfluxDB, Grafana
- 가상환경: (venv / conda 기입)
- 저장소: (GitHub URL 기입)
- 로컬 사양: (RAM __GB, OS, WSL 여부)

## 2. 폴더 구조 (계획)
```
amhs-realtime-monitoring/
├── data/                      # git 제외
│   ├── raw/                   # 원천·라벨링 원본
│   └── processed/             # aihub.parquet, 특징 데이터
├── notebooks/
│   ├── 01_build_check.ipynb   # 통합 결과 확인
│   ├── 02_timeseries_check.ipynb
│   ├── 03_eda.ipynb
│   ├── 04_features.ipynb
│   ├── 05_models.ipynb
│   └── 06_alarm_policy.ipynb
├── src/
│   ├── config.py              # 경로, 상수, 윈도우 길이 등
│   ├── build_dataset.py       # JSON → aihub.parquet
│   ├── features.py            # 슬라이딩 윈도우 특징 (학습·추론 공용)
│   ├── train.py               # 학습 + ONNX 저장
│   ├── evaluate.py            # 재현율, 비용, 조기 감지 등
│   └── alarm.py               # 알람 규칙 (학습·추론 공용)
├── app_mvp/
│   └── dashboard.py           # 8주차 Streamlit MVP
├── services/
│   ├── replayer/              # Validation 재생 → MQTT 발행
│   ├── inference/             # MQTT 구독 → 특징 → 판별 → InfluxDB
│   ├── grafana/               # 대시보드 JSON, 알람 설정
│   └── mosquitto/             # 브로커 설정
├── models/                    # model.onnx 등
├── reports/figures/
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## 3. 데이터 흐름
```
[학습]  raw JSON → build_dataset.py → aihub.parquet → features.py → train.py → models/model.onnx
[실시간] Validation → replayer → MQTT(amhs/{type}/{device}) → inference(features.py + model + alarm.py)
        → InfluxDB → Grafana
```
- MQTT 토픽 예: `amhs/agv/04`, `amhs/oht/12`
- 메시지 예: `{"device":"agv04","ts":"09-03 10:49:54","NTC":25.7,...,"ir_max":43.6,"state_true":0}`

## 4. 주요 모듈·함수 (구현하면서 채우기)

| 파일 | 함수 | 역할 | 입력 → 출력 |
|---|---|---|---|
| src/build_dataset.py | `parse_json()` | JSON 1개를 한 행으로 변환 | 경로 → dict |
| src/build_dataset.py | `build()` | 전체 통합 | 폴더 → aihub.parquet |
| src/features.py | `make_features()` | 최근 N초 특징 계산 | 장치별 버퍼 → 특징 벡터 |
| src/alarm.py | `AlarmState.update()` | 연속 N초 규칙 적용 | 예측 등급 → 알람 이벤트 |

## 5. 코딩 규칙
- 노트북은 탐색용, 재사용 코드는 src/로 이동
- 특징 계산과 알람 규칙은 학습·실시간이 같은 함수를 사용 (불일치 방지)
- 경로·상수는 config.py에서만 관리
- data/, models/의 대용량 파일은 .gitignore
- 난수 시드 고정 (42)
- README용 그래프는 reports/figures/에 저장

## 6. 알려진 이슈·메모
-
