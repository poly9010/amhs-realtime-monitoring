# 00. 프로젝트 개요

> 프로젝트의 목적과 전체 계획을 담은 고정 문서입니다. 큰 방향이 바뀔 때만 수정합니다.

## 1. 한 줄 요약
반도체 팹 물류 자동화(AMHS) 이송장치(OHT·AGV)의 1초 단위 센서·열화상 데이터로 탄화(과열) 위험 등급을 판별하고, 저장 데이터를 실시간처럼 재생해 40대 이송장치의 상태를 한눈에 보여주는 **실시간 예지보전 모니터링 시스템**을 만든다.

## 2. 배경과 목적
- **목적**: 취업용 포트폴리오 (목표 회사: 세메스 SEMES)
- **세메스와의 연결점**: 반도체 장비 및 물류 자동화(OHT 등) → 이송장치의 상태 진단과 실시간 모니터링 역량을 보여준다
- **제약 조건**: 직접 수집 가능한 센서 없음 → 공개 데이터 재생 방식, 로컬 PC(약 16GB RAM) 중심

## 3. 사용 데이터
- **AI Hub 「제조현장 이송장치의 열화 예지보전 멀티모달 데이터」**
  - 대상: OHT 20대(OHT-OCS), AGV 20대(Mir-100, 저상용 AGV)
  - 1차 개방: 13,121세트 (OHT 5,948 / AGV 7,173)
  - 1세트 = 1초 측정값 = 센서 CSV + 열화상 BIN + 라벨 JSON
  - 센서: NTC(온도), PM1.0·PM2.5·PM10(미세먼지), CT1~CT4(전류) + 열화상 최고온도
  - 정답: 0 정상 / 1 관심 / 2 경고 / 3 위험 (위험은 약 8%로 적음)
  - 구축기관 기준 성능: Accuracy 0.80, Macro F1 0.68 → 비교 기준선으로 사용
  - 한계: 등급별 데이터는 테스트베드 모사 시스템에서 수집 → README에 명시
- **서브(선택)**: DCASE ToyTrain / MIMII Slider 소리 이상탐지 (메인 완성 후 여유 시)

## 4. 최종 산출물
1. JSON → Parquet 통합 파이프라인
2. 실시간 계산 가능한 슬라이딩 윈도우 특징 + 등급 판별 모델(LightGBM 중심, ONNX 경량화)
3. 알람 정책(연속 N초 규칙 등)과 그 근거
4. **8주차 MVP**: Streamlit 기반 실시간 모니터링 (40대 동시 재생, 배속)
5. **최종 시스템**: 재생기 → MQTT → 추론 서비스 → InfluxDB → Grafana, Docker Compose로 한 번에 실행
6. README, 시연 영상, 발표 자료

## 5. 12주 로드맵

| 주차 | 단계 | 핵심 작업 | 산출물 |
|---|---|---|---|
| 1 | 데이터 준비 | 다운로드·분할압축 병합(WSL), 데이터 설명서 정독, JSON 전체를 표 하나로 통합 | aihub.parquet, 통합 스크립트 |
| 2 | 시계열 복원·검증 | 장치별 시간순 정렬, 끊김 구간 확인, 등급이 시간에 따라 변하는지 확인 | 검증 리포트, 재생 방식 결정 |
| 3 | EDA | 등급별 센서 분포, OHT vs AGV, 등급 전환 직전 변화, 열화상 최고온도와 NTC 관계 | EDA 노트북 |
| 4 | 실시간용 특징 | 최근 N초 평균·기울기·변화량, 장치 단위 분할 | 특징 모듈, 데이터셋 |
| 5 | 모델 1차 | 임계값 규칙 베이스라인 → LightGBM | 성능표 (기준선과 비교) |
| 6 | 평가·알람 정책 | 위험 재현율, 오분류 비용, 연속 N초 알람 규칙, 조기 감지 시간 | 알람 정책 문서, 최종 모델 |
| 7 | 경량화 (+선택: 이미지) | ONNX 변환, 추론 시간 측정 / 여유 시 BIN 열화상 활용 | 추론 속도 비교표 |
| **8** | **MVP** | Streamlit에 재생기+모델+현황판 통합, 40대 동시 재생·배속 | **동작하는 모니터링 시스템** |
| 9 | 구조 분리 ① | 재생기 → MQTT(Mosquitto) → 추론 서비스, 장치별 토픽 | 메시지 기반 파이프라인 |
| 10 | 구조 분리 ② | InfluxDB 저장, Grafana 대시보드·알람, Docker Compose | docker compose up 한 줄 실행 |
| 11 | 안정화·시연 | 결측·지연 처리, 시연 시나리오, 영상 촬영 | 시연 GIF·영상 |
| 12 | 문서화 | README, 아키텍처 설명, 발표 자료, 면접 대비 | 최종 포트폴리오 |

## 6. 원칙
- **8주차 MVP 우선**: 이후 일정이 밀려도 제출 가능한 결과물을 확보한다
- **실시간 계산 가능한 특징만 사용**: 현재까지 들어온 최근 N초만 사용, 학습·추론 공용 코드
- **학습은 Training, 재생 시연은 Validation**: 내부 검증 분할은 장치 단위
- **평가**: 정확도보다 위험·경고 재현율, 오분류 비용, 알람 품질(오경보, 조기 감지)
- **판단 근거를 기록**: 모든 주요 결정은 03_의사결정로그.md에 남긴다
